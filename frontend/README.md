# Frontend - Smart Study Hub

**Task 5: User Registration Form**

This folder contains the React frontend implementation for Task 5:
- Registration Form for users (name, email, password)
- Uses React state to handle form input
- Styled using `RegistrationForm.css`

Run the app:
```bash
cd frontend
npm install
npm start
